.login-container
  .login-form
    .title
      .badge Iniciando sesión
    .body
      = bootstrap_form_tag(url: '/users/sign_in') do |f|
        = f.text_field :user, label: 'Cedula'
        = f.password_field :password, label: 'Contraseña'
        = f.submit 'Log In', class: 'btn btn-primary pull-right'

      
